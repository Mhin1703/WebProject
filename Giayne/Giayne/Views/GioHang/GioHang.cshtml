@using Giayne.Models;
@model List<GioHang>

@{
    ViewBag.Title = "GioHang";
    Layout = "~/Views/Shared/USER.cshtml";
}

<center><h2 class="text-info">GIỎ HÀNG CỦA BẠN</h2></center>

<div class="card border-dark">
    <table class="table">
        <tr style="background-color:lightblue">
            <th>MÃ SẢN PHẨM</th>

            <th>TÊN SẢN PHẨM</th>

            <th>ẢNH SẢN PHẨM</th>

            <th>SỐ LƯỢNG</th>

            <th>GIÁ BÁN</th>


            <th>THÀNH TIỀN</th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
    <tr>
        <td >@item.masp</td>
        <td >@item.tensp</td>
        <td ><img src="~/hanh/@item.anh " width="50%" /></td>
        @*@using (Html.BeginForm("CapNhatGH", "GioHang", new { @MaSP = item.masp }))
        {*@
            @*<td>@Html.TextBox("txtSoLuong", item.sl) </td>*@
            <td >@*<input type="number" min="1" name="txtSL" value="@item.sl" size="2" />*@@item.sl </td>
            <td >@String.Format("{0:0,0}", item.giaban)</td>
            <td >@String.Format("{0:0,0}", item.thanhtien)</td>
            <td>
            @*<button class="btn btn-link" type="submit">@Html.ActionLink("Xóa Sản Phẩm", "XoaGH", new { @MaSP = item.masp })</button>*@
            @*<br />
                <input type="submit" value="Cập Nhật" class="btn btn-primary" />
            </td>*@
        @*}*@
    </tr>
            <tr>
                <td colspan="6" style="text-align:right">

                 <button class="btn-success" >@Html.ActionLink("Chỉnh sửa giỏ hàng", "SuaGioHang")</button>
                </td>
              
                <td colspan="6" style="text-align:right;color:blue">
                    @using (Html.BeginForm("DatHang", "GioHang", new { @class = "" }))
                    {
                        <input type="submit" value="Đặt hàng" class="btn-danger" />
                        }
                    
                </td>
                
            </tr>
        }

        @*<tr style="background-color: lightblue">
            <td colspan="3" style="color:blue; font-weight:bold;text-align:right;">Tổng cộng:</td>
            <td style="color:red; font-weight:bold;">@ViewBag.TongSL</td>
            <td></td>
            <td style="color:red; font-weight:bold;">@String.Format("{0:0,0}", ViewBag.TongTT) VNĐ</td>
        </tr>*@
    </table>
</div>


